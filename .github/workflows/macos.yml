name: CI

on:
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: macos-latest
    
    steps:
    - name: Message
      run: |
        for i in `seq 900`;
        do
          echo "Hello $i"
          sleep 2
        done
